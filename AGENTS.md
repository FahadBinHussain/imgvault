# imgvault

chrome extension (mv3, no build for ext itself; vite build outputs to `dist/`) + tauri rust native host for yt-dlp downloads.

- native host source: `native-host/src-tauri/src/main.rs`; build with `cargo build --release --target x86_64-pc-windows-gnu` and copy the exe to `native-host/ImgVault-Native-Host.exe`; rebuild + copy after every rust change, not just compile.
- extension edits: bump `"version"` in `nextgen-extension/public/manifest.json`, then `pwsh tools/reload-extension.ps1` (rebuilds dist via `pnpm build`, opens `public/reload.html`, bg blanks its tab + self-reloads — manifest bumps picked up too; Extensions Reloader is JS-only and never re-reads the manifest).
- push: remote is `https://github.com/FahadBinHussain/imgvault.git` (capital F/B). token via rule 34 (Read-VaultSecret, github.com* pattern — never use a token.txt; github tokens are vault-native). `git push "https://x-access-token:$tok@github.com/FahadBinHussain/imgvault.git" main`.
- terabox video provider (2.9.0, 2026-08-27): `TeraBoxUploader` in uploaders.js implements the PCS chunked upload protocol (precreate → locateupload → superfile2 chunks at 4MB → create), cookie auth (`ndus`+`browserid`+`lang` from the logged-in session via `chrome.cookies.getAll({domain:'terabox.com'})`, optional `teraboxCookie` settings override). jsToken is scraped from the www.terabox.com homepage **with a bare UA fetch — no session cookie and no Referer header** (the cookie OR a `www.terabox.com/` Referer redirects to a `simple-verify` captcha gate that has no jsToken; only the bare fetch returns the landing page with the `fn("...")` jsToken wrapper — verified 2026-08-28; the same-page Referer trick does NOT work, 2.11.9→2.11.10). Auto-refreshed on `errno 4000023`. **API calls stay on dm.terabox.com** — only the token scrape uses www (2.11.8; the intermediate 2.11.7 moved the whole base to www and broke `/api/list` with `errno -6`/0-found). 2.11.9 removed the Cookie header from the token scrape to avoid the captcha redirect. storage uses `extra_metadata.videoHosts.terabox` (no dedicated schema column, unlike filemoon/udrop) — the generic `normalizeVideoHosts` round-trips it fine. note: terabox large-file downloads are CDN-throttled (~30KB/s), so it's an upload host; the download dlink comes from `/api/filemetas?dlink=1&origin=dlna` (crack mode). `plan-terabox-vault.md` has the full investigation.
- the CI workflow (`.github/workflows/nextgen-extension-crx.yml`) must reference `manifest.json` as `public/manifest.json` (5 spots: extension version read, copy step, zip name, crx name, native-host version read) — the root `nextgen-extension/manifest.json` was removed in 2.1.7 and every build failed with "Cannot find module './manifest.json'" until fixed (commit 7412db6). vite already copies `public/` into dist, so the workflow's manual `cp` only needs the manifest path corrected.
- vault items are soft-deleted to trash (un-vault + moveToTrash with `skipCollectionCount: true`); trash cards fail gracefully via onError placeholders + 12s timeout. **2.12.77 restore bug (items vanished)**: un-vaulting at delete meant restore-from-trash produced an encrypted `.bin` gallery item with NO provider URLs — never back in the vault, and in the gallery at best an unplayable broken card (empty strict-host link), so users experience it as "gone". Fix: `restoreFromTrashNeon`/firestore `restoreFromTrash` re-vault any trashed row whose `encryptedBlobUrl` is set (isVaulted true + vaultMode hidden + fresh vaultedAt; encrypted fields ride through updateImageNeon untouched — verified by the delete path writing isVaulted:false while keeping the blob) and skip the collection-count bump for those (vaulted items are excluded from counts, symmetric with delete). Also the SW `restoreFromTrash` case reported `success:true` even when the method returned false (id not in trash → card disappeared while the row stayed deleted) — now a loud `success:false`. Repaired one orphaned row (b2ebceb4…, re-vaulted via SQL) and one vault video still trashed (e64083c3…, restores correctly with the fix).
- resolve page integrity checks (udrop/filemoon) must merge `vaultImages` with `images`: `useImages` filters out vaulted items, so without the merge their codes never count as referenced and their files show up as false orphans on the integrity tabs (was a real bug 2.1.3).
- **UDrop integrity false-broken storm (2.12.78)**: a failed `/folder/listing` call used to return `null` and be silently skipped, so `listAllUdropFiles` handed back an EMPTY list that both checks treated as success — every video with a working UDrop link flipped to "Broken links" (showed 99 broken / 0 found / 0 extra while the real numbers were 99 found / 0 broken), and scene items with codes got pushed into `found` with no verification. fix (no-fallback rule): `listUdropFolder` now THROWS on HTTP/`_status` failure (name + folder in the message), `checkUdropIntegrity`/`checkSceneIntegrity` let it propagate into the tab's red error box — a check only renders counts when the full listing actually landed. also: the shared `notice` toast carried across tabs (a "UDrop 3D check" toast rendered under the video "UDrop file integrity" heading, which is what made the numbers look self-contradictory) — `setNotice(null)` now runs on any tab/subtab switch, and the udrop video tab renders success toasts in success colors like the others. vault `.bin` files on UDrop are no longer false orphans: `udropUrlsOfItem()` adds `encryptedBlobUrl` to the referenced-URL set on both the video and scene extra passes.
- **Resolve-route audit (2.12.79)**: swept every integrity route for the 2.12.78 bug class + cross-tab double-counting. (1) Same silent-listing failure existed on TeraBox (`checkTeraBoxIntegrity`/`checkTeraBoxSceneIntegrity` swallowed `listAllTeraBoxFiles` errors — video tab marked every linked item broken; scene tab FAKED `found` entries) and Filemoon (`checkFilemoonIntegrity` fell back to per-file probing with hidden `extra=0`); all three now throw into the tab's error box, `listTeraBoxFolder` throws on any page/errno failure (resolvers keep a `listTeraBoxFolderSafe` wrapper returning [] so vault-stream keeps its established `''`-on-failure contract), and every catch clears that tab's stale counts so error and numbers never render together. (2) **3D items belong on the 3D tab only**: scene files (.spz + all model extensions `MODEL_EXT_RE`, plus texture images `SCENE_TEXTURE_EXT_RE` — the scene tab claims every texture orphan) are skipped in the video-tab extra passes on UDrop and TeraBox, so the 12 orphan scene groups no longer pollute "Extra on UDrop" videos. Rule: every host file is claimed by exactly ONE tab (model/texture ext → 3D tab, else video tab). (3) TeraBox video extras previously matched ONLY the filtered video items' ids/names — scene spz, link items, and vault blobs on TeraBox were false "Extra on TeraBox"; now `checkTeraBoxIntegrity(items, allItems, cookie, onProgress)` builds the referenced set via `collectTeraBoxRefs()` (exported; covers videoHosts/extraMetadata copies, legacy columns, dlink `?fid=`, sceneFiles, `encryptedBlobUrl`/`encryptedBlobFileId`). UDrop side got the same allItems + encrypted-blob referencing in 2.12.78. (4) All terabox video-tab entries now carry `codes: []` (2.12.56 render rule).
- **Orphan delete buttons on every host tab (2.12.80)**: TeraBox and Filemoon video-tab extra cards gained Delete (UDrop video + both 3D tabs already had it) — symmetry rule: every extra/orphan card everywhere must offer delete. TeraBox deletes by EXACT `file.path` via `deleteTeraBoxFiles` (recycle bin, and the card refuses to render delete without a path — never name-based); Filemoon uses `deleteFilemoonFile` (permanent, confirm says so). Both reuse the `deletingOrphans` spinner map and drop the entry from local state on success.
- **Preferences bound to the DB (2.12.81)**: Settings → Preferences (default image/video/3D source) now actually round-trips through `public.settings`. Two bugs: (1) the live table never had `default_3d_source` (schema.ts declared it, no migration ran), so `saveUserSettingsNeon`'s INSERT threw 42703 on EVERY sync — the page's catch swallowed it as "cloud sync failed", so even the API-key push was dead; column added live `alter table public.settings add column if not exists default_3d_source text not null default 'udrop'` — schema.ts changes now REQUIRE a matching live migration, the repo schema is not auto-applied. (2) `handleSave`'s cloud-sync gate only fired when an API key field was non-empty, so changing just the three dropdowns saved locally and never pushed; gate now includes gallerySrc/videoSrc/threeDSrc. Restore side (SettingsPage load effect) already worked — values land in chrome.storage.sync which every page reads via useChromeStorage. Live DB values: imgbb / terabox / terabox.
- resolve 3D scene checks must read the companion texture refs, not just the spz (2.12.53): scene uploads store the texture ONLY as `textureUrl` (+ sizes) — `checkTeraBoxSceneIntegrity` used to read spz-side filename/id fields only, so every scene thumbnail (`thumbnail_*.webp`, stem never matches the spz) showed as a standalone "extra" with the false "its .spz is gone" copy. fix: `collectTextureRefs` feeds `textureUrl` basenames + `extraMetadata.sceneFiles.{spz,texture}.{fileId,filename}` into both the referenced and db sets; the GalleryPage `uploadSceneDirect` fallback persists `extraMetadata.sceneFiles` (merge-safe via buildExtraSeed); standalone-texture copy now says no matching .spz was found. udrop side already read `textureUrl` codes, no change needed there. **2.12.55**: cross-host symmetry — both 3D checks now receive ALL scenes (was: pre-filtered to already-linked ones, so udrop-only scenes were invisible on the terabox tab). Foreign scenes land in `no url`, exactly like the video tabs. **2.12.56**: blank-page crash — the 2.12.55 wider input exposed that terabox scene `missing` entries carry no `codes` while the scene row renders `codes.length` (TypeError unmounts the whole tree). Foreign scenes slipped into `missing` because the gate counted generic `spzUrl` as a link. Fix on both layers: `hasTeraboxRef` (host-specific only) decides missing vs noUrl, all terabox pushes carry `codes: []`, and the render guards `(codes || []).length`. Rule going forward: every integrity entry in every bucket must carry `codes` (array, possibly empty) — the filemoon/udrop checks already do. **2.12.57**: scene rows now have a Fix button (noUrl + missing) — `SceneFixModal` at the bottom of ResolvePage picks .spz (+ optional texture/config), `runSceneFix` uploads via `VIDEO_UPLOAD_SERVICES[*].uploadWithProgress` (direct XHR, no 64MiB limit) and `updateImage`s the EXISTING row in place (udropWatchUrl/teraboxWatchUrl/spzUrl/textureUrl/configJson), no duplicate item. Missing-scene status labels are host-aware now. **2.12.58**: symmetry — scenes are 1 unit = SPZ + Image; both checks now require BOTH files (partial = missing) and every scene card (Found/Missing/NoUrl) renders both file rows (SPZ + Image) with per-file found/missing + host-aware copy; `udropApi.checkSceneIntegrity` and `teraBoxApi.checkTeraBoxSceneIntegrity` now track spzMatched/texMatched separately (incl. `?fid=` id extraction for TeraBox dlinks) instead of any-one-file. **2.12.59**: default 3D source preference — `default3DSource` (chrome.storage.sync, `udrop`/`terabox`, default `udrop`) in `providerCatalog` (`THREE_D_SOURCE_OPTIONS`, `DEFAULT_3D_SOURCE`), Settings → Preferences → Default 3D Source (Box icon), `schema.ts` + `storage.js` (firestore + neon `default_3d_source`) both carry it; Resolve 3D subtab auto-initializes from it, Gallery 3D upload toggle preselects it. **2.12.60**: scene Fix is auto — like video Fix, `startSceneFix` downloads SPZ (+ texture) from the host where the scene lives (fresh dlink via `resolveTeraBoxPlaybackUrl` / `vaultDownloadUrl` when stored URL is stale) and re-uploads to the missing host via `uploadWithProgress` (no 64MiB limit), updates the EXISTING row (per-host watchUrl + spzUrl/textureUrl + `extraMetadata.sceneFiles[host]`), falls back to `SceneFixModal` only when no source file exists locally; button shows spinner via `fixProgress[item.id]`. **2.12.61**: udrop scene Fix now refreshes stale `download_token` URLs via `extractUdropCode` → `POST /file/download {short_url}` (covers `town-500k` / `anime-train-500k` where `sceneSpzFileId` was never stored, so the old token 404s — verified `OHiG`/`OHdA` still exist on UDrop via `file/download` giving fresh `download_url`). **2.12.62**: viewer cache stuck at 32% — `scene-viewer.html` header now has Clear-cache button (`clearCacheBtn` → `clearViewerCache(id)` + reload), `scene-viewer.js` auto-clears truncated blobs (<1KB) on cache-hit before they stall the `SplatMesh` loader. **2.12.63**: the stuck-32% case hid the header buttons — the full-screen loading overlay (z-index 20) covered the header (z-index 10). header raised to z-30 and an inline (module-independent) `<script>` wires `clearCacheBtn` so it works even when the viewer module died during load. **2.12.64**: clear-cache button icon changed to a broom (lucide `broom.svg` paths). **2.12.65**: gallery scene cards show the scene's own `textureUrl` as its thumbnail (`CachedImg` `thumbKey scene-${id}`, object-cover, 3D badge overlay) instead of a generic gradient; no-texture scenes keep the Box + `3D Scene` placeholder. **2.12.66**: terabox-scene thumbs break after ~8h because the stored `textureUrl` is a single-use signed dlink (`dm-d.terabox.com`) — gallery lazy-refreshes them like video thumbs (`sceneTexThumbs` effect: `?fid=` → fsId → `getVideoPlaybackUrl terabox` → fresh dlink → state + persisted via `updateImage`); card `thumbKey` includes the URL tail so the cache invalidates on refresh. **2.12.67**: scene config shape — worldlabs ships TWO json shapes (nested raw dump `{camera,scene,controls}` vs flat `{position,rotation,offset,cameraRadius,radius,duration}`); `utils/sceneConfig.js` exports `flattenSceneConfig` (maps `controls.camera_radius`→cameraRadius w/ `camera.position[2]` fallback, `orbit_radius`→radius, `duration`) and it's applied on ALL write paths (SW uploadScene, Gallery uploadSceneDirectly, Resolve runSceneFix + auto-fix pass-through). DB flattened in-place 2026-09-10; revert SQL kept OFF-REPO at `C:\Users\<user>\Downloads\config-json-backup-20260910_132805.sql` (private data rule: no DB row dumps / internal ids in the public repo). **2.12.54**: exact match — terabox dlinks embed `?fid=<vuk>-<app>-<fs_id>` and the trailing segment IS the fs_id (DB-verified on `bayazid549@gmail.com`/weathered-wave-41213107: the Heavenly texture dlink ends in `621029115057028` = `thumbnail_1440.webp`; size 67104 matches too). `teraBoxFsIdFromUrl` feeds both id sets, so opaque `/file/<hash>` dlinks resolve with no filename. neon account owning the imgvault DB: `bayazid549@gmail.com`, project `imgvault` (`weathered-wave-41213107`) — query via `neon-account.ps1 run bayazid549@gmail.com connection-string --project-id weathered-wave-41213107 --role-name neondb_owner`, never print the DSN.
- `retryVideoHostUpload` iterates ALL fallback source candidates until one returns a real video blob — watch pages (text/html) and expired direct links are common, so the retry must never stop at the first candidate (was a real bug 2.1.6). filemoon `/d/`/`/e/` URLs only serve HTML: resolve them via `api.byse.sx/file/direct_link` (`getFilemoonDirectLink`) and fetch the CDN url with the `https://filemoon.sx/` referer (2.1.7). if the account key lacks direct_link permission (`Byse /file/direct_link rejected: Invalid operation`) or hls/link requires Premium API (`This function is not allowed because you've not enabled Premium API`), the anonymous SPA player flow still works and is the reliable path (2.2.0): `filemoonSpa.js` does `POST /api/videos/{code}/captcha` -> solve local proof-of-work (custom ARX hash on SHA-256 constants, ported from the site's pow-*.js bundle: `powHash(nonce:counter)` with leading-zero-bits >= difficulty) -> `POST /api/videos/{code}/captcha/verify` (X-Captcha-Token not needed here) -> `POST /api/videos/{code}/playback` with a dummy `{fingerprint:{token,viewer_id,device_id,confidence}}` + `X-Captcha-Token: <verify token>` -> AES-256-GCM decrypt (WebCrypto; key = base64 key_parts selected by version map `n -> [n, 31-n]`, out of range = all parts; tag appended to payload) -> `sources[]` HLS m3u8 signed URL -> `downloadHlsAsVideoBlob`. the /d/ download page itself is NOT scriptable (countdown + recaptcha + fingerprint attestation), and yt-dlp refuses filemoon outright (`[Piracy] this website is no longer supported`), so player-flow HLS is the only automated recovery path. verified end-to-end 2026-08-15 against filecode z10yjnr2i3ch (segment `seg-1-v1-a1.ts`, 150776 bytes, MPEG-TS).
- manifest source of truth is `nextgen-extension/public/manifest.json`; vite copies it into dist, and postbuild.ps1 must copy from `public/` — the old root `manifest.json` was stale at 2.0.0 and silently overwrote dist on every build (removed; postbuild fixed 2.1.7). if the loaded extension version on edge://extensions doesn't match the manifest, check dist/manifest.json first.
- byse `/file/list` caps at 100 files per page and does NOT send a `total` field — the count is `result.results_total` (also `results`/`pages`). reading `.total` makes pagination stop at page 1, so once the account passes 100 files every healthy video beyond the first page shows up as broken on the resolve integrity check and the page-2+ files vanish from `extra`. fixed 2.2.4 in `listAllFilemoonFiles`; keep the `files.length < perPage` break guard if the API ever stops reporting totals. verified live: 111 files, page 2 returns the remaining 11.
- TeraBox `/api/list` has the SAME 100-per-page cap, and `listTeraBoxFolder` (teraBoxApi.js) originally fetched only `num:100,page:1` — once the account passed 100 files, everything beyond page 1 showed as "broken links" on the resolve integrity check while still playing fine (files existed, just never listed). fixed 2.11.19: paginate until a page returns `< num` (with a `page > 100` safety break). the `[teraBoxApi] Listed N TeraBox files.` console log printing exactly `100` is the tell — the DB has more files than that.
- ukdevilz.com mirrors VK videos; its own `videofile/{id}.mp4` is stale (Cloudflare challenge/404) and generic-extractor downloads fail. `rewrite_mirror_url()` in main.rs rewrites `/watch/{owner_id}_{video_id}` to `vk.com/video-{owner}_{video}` so yt-dlp uses its vk extractor. id format is `owner_video` with an underscore — the id filter must allow `_` (was missed once). apply to both download paths if adding url handling. same trick likely works for other vk mirrors.
- `--impersonate chrome` is added for all downloads (tiktok + generic hosts).
- host survives extension reloads: the native host is single-threaded (download runs inline in the message loop), so when the extension port dies mid-download it never notices until the download finishes — the file still completes in the background. the host writes a completion journal (`%TEMP%\imgvault-yt-dlp-journal-{requestId}.json` with status completed/failed) after every download; on startup the service worker reads it via the `read_download_journal` host action and adopts the real outcome (`completed` resumes the pending auto-upload flow). if the journal is missing the original host is still busy — the record stays `running` with `recoveredFromReload: true` and HostPage polls the journal every 5s. `delete_download_journal` also cleans the pid file. this is the only correct recovery path — never mark a reloaded download `interrupted` while a journal could still appear.
- lint broken (no eslint config), pre-existing.
- proton vpn blocks the extension SW's neon writes too (not just my psql): `updateImage`/Fix/link actions hang or silently no-op while proton is on — the "Link to item" click that "did nothing" (2.2.6) was this. the extension has no proxy path (mv3); workaround = psql directly through the `automata\tools\neon-via-proton` relay (see that tool's AGENTS.md; imgvault endpoint `ep-silent-cherry-a1th414c.ap-southeast-1.aws.neon.tech`, port 5435 relay) or disable proton/split-tunnel while doing DB actions. relay gotcha: the PROXY group is a url-test over ~2900 mostly-dead `t.me/ForceRunVPN` free nodes and flips between healthy and RISKY nodes — socks egress tests fail intermittently; just retry psql after re-selecting a healthy node (`GET/PUT /proxies/PROXY` on controller 10813). never diagnose the relay with `curl telnet://` — curl hangs on telnet-over-socks negotiation regardless of connectivity; use a real request (google 200) as the egress probe. **CORRECTION 2026-08-19**: Proton blocks only non-443 egress (psql 5432 direct times out; curl/WSS 443 to the same host works 3/3 with Proton on) — the extension's WSS 443 Neon writes work DIRECT with Proton on. the 2.2.6 "link did nothing" hangs were a dead socks node + PAC routing neon.tech into it, not Proton. never enable the browser PAC while a dead node is selected; disable the PAC and test direct first (see neon-via-proton AGENTS.md).
- resolve page notices render on the videos tabs ONLY when `notice.type === 'error'` (success messages from "Link to item" were invisible), and the link handlers set the success notice BEFORE `run*IntegrityCheck()`, which starts with `setNotice(null)` and wipes it. fixed 2.2.7: notices render for any type on both video tabs, and the success notice is set AFTER the check re-run. link flow writes `filemoon_watch_url=https://filemoon.sx/d/{code}` + `filemoon_direct_url=https://filemoon.sx/e/{code}` (udrop: `https://www.udrop.com/file/{code}` both) via `updateImage`.
- after "Link to item" the re-run check used STALE `images`/`vaultImages` hook state — the just-linked item still lacked the URL in memory, so the orphan stayed listed right after linking (and external DB edits like psql links show as false orphans until page refresh). 2.2.8 added `Promise.all([reloadImages({ silent: true }), reloadVaultImages()])` after `updateImage`, but the orphan STILL persisted even with the write landing in Neon — the running extension simply wasn't the new build (the `http://reload.extensions` reload hadn't taken; always verify the loaded version on edge://extensions matches dist/manifest.json). fixed properly in 2.2.9: `runFilemoonIntegrityCheck`/`runUdropIntegrityCheck` now fetch FRESH items themselves (`sendMessage('getImages')` + `sendMessage('getVaultImages')`) instead of trusting hook state, so every check run sees current DB state regardless of page state or build-timing.
- the REAL orphan root cause (2.3.0): "Link to item" called `updateImage` with `filemoonWatchUrl`/`filemoonDirectUrl`, which OVERWROTE the item's single URL pair — linking a second file to the same item re-orphaned the first one (e.g. ve08iog0e6ku <-> 8hwf0abdvodt flip-flopped on `de28c36f` as each new link replaced the previous). fix: items now hold MULTIPLE links per provider in `extra_metadata.filemoonLinks` / `udropLinks` (array of `{filecode, watchUrl, directUrl}`); `storage.linkProviderFileToItem(id, providerKey, link)` (new SW message `linkProviderFileToItem`) APPENDS to that array and only fills the legacy primary columns when empty (idempotent per filecode); both integrity checks read the arrays in addition to all legacy fields, and the resolve page filters count them too. after 2.3.0 the orphan list is accurate: a file is extra only if NO item (live or vaulted) references its code.
- trash items don't count: `getAllImagesNeon` filters `deleted_at is null` (vault merge adds vaulted items), so a filemoon/udrop code whose only referencing item is soft-deleted shows as orphan forever, and the UI can't link it (match search also skips trash) — restore the item from the Trash page to clear it, or delete the file from the provider account.
- when the socks relay is down AND Proton's DNS stops resolving `*.neon.tech` (happens 2026-08-19; system DNS returns only AAAA, `neon.tech` NXDOMAIN), the extension still works (Chrome DoH) but psql/curl die. working fallback: curl the neon-http endpoint DIRECT over 443 with IP pinning — `curl --resolve "ep-silent-cherry-a1th414c.ap-southeast-1.aws.neon.tech:443:52.220.170.93" -X POST "https://ep-silent-cherry-a1th414c.ap-southeast-1.aws.neon.tech/sql" -H "Neon-Connection-String: postgresql://neondb_owner:<pw>@<host>/neondb" -H "Neon-Raw-Text-Output: true" -H "Content-Type: application/json" --data-binary @query.json` — body `{"query":"..."}`, JSON-escaped SQL. real A records (via cloudflare-dns.com DoH): 13.228.184.177 (dead), 13.228.46.236, 52.220.170.93 (both live). no `/session` endpoint needed for password auth — the `Neon-Connection-String` header carries it.
- vault encryption (2.6.0, 2026-08-25): `vaultCrypto.js` (pure WebCrypto AES-256-GCM + PBKDF2 + key wrapping), `vaultSession.js` (page-side key in memory), `vaultSetMasterKey`/`vaultClearMasterKey` SW messages, `handleVaultedUpload` encrypts before upload, `moveItemToVault` encrypts on move (requires unlock; throws if locked), `restoreFromVault` decrypts -> re-uploads via normal providers, `decryptVaultBlob` regenerates stale udrop download URLs from fileId. legacy vaults auto-upgrade on first unlock. legacy vaulted items (pre-2.6.0) stay unencrypted per option B. `plan.md` has the full architecture.
- vault upload progress-log cadence (2.8.0, 2026-08-26): the in-page `uploadVaultedDirectly` progress callback MUST gate its log line on BYTES (`loaded - lastLoggedBytes >= ~20MB`), never on percent or on a string `pct`. percent-gating silently drops every log line between 0% and 100% for files >~1GB (10% of 1.2GB = 128MB = ~39s of silence at ~3.3MB/s), so the live log looks frozen at "0% (363 KB / 1.2 GB)" while the upload actually runs. a progress callback that builds a display label first is the classic trap (string `pct` -> `pct - uploadLogPct` = NaN -> log never fires). ALSO: udrop accepts large single PUTs fine — verified 50MB=13.5s, 512MB=195s, 1.2GB=387s (~3.3MB/s), all through `POST /api/v2/file/upload` with `upload_file` FormData. there is NO per-upload size cap on the server for a single request; the "stuck upload" symptom is a CLIENT log-timing illusion. udrop `api/v2` has no working `file/list` method (only `file/delete`) — file enumeration isn't available via API, so vault orphan cleanup must use file_ids captured from upload responses.
- **STRICT provider symmetry — no fallbacks (2.11.x)**: user forbids fallback chains for anything. resolve every provider-derived value (video playback, modal source, poster/thumbnail, images) ONLY through the host selected in settings (`defaultVideoSource` / `defaultGallerySource`) via `getStrictVideoProviderLink` / `getStrictImageProviderLink`. never use the chain-based `getPreferredVideoProviderLink` / `getPreferredImageProviderLink` for UI. empty poster when the selected host has no thumbnail (udrop/terabox have none) — don't backfill from another host.
- filemoon `/e/` URLs are embeddable player PAGES, not video files — gallery modal plays them in an iframe, never a `<video src>` (2.10.6).

## video hosts — file type support matrix (literal, evidence-backed)

status legend (apply everywhere in this repo): `✓` = proven/works (observed in DB or a real run), `✗` = structurally won't accept (video-only host, etc.), `?` = not tested yet — never assume `?` means yes or no, and mark results back here after a test fills one.

| extension | Filemoon | UDrop | TeraBox |
|---|---|---|---|
| `.mp4` | ✓ | ✓ | ✓ |
| `.mkv` | ✓ | ✓ | ✓ |
| `.bin` | ✗ | ✓ | ✓ |
| `.webm` | ? | ? | ? |
| `.mov` | ? | ? | ? |
| `.avi` | ? | ? | ? |
| `.ts` | ? | ? | ? |

context: `✓` rows come from the actual DB — 82 `.mkv` (video/matroska) + 17 `.mp4` on all three hosts; `.bin` on UDrop is the encrypted vault blob (`encryptAndUploadVaultedBlob`, background.js:1146). filemoon is a video-only host, so non-video = `✗`. TeraBox `.bin` marked `✓` 2026-08-30: the PCS precreate API accepted the `.bin` filename (responded `4000023` need-verify/auth-only — stale cookie — never a file-type rejection), and PCS is byte-agnostic: it never checks extensions at precreate/create, it just uploads bytes. every other extension is untested (`?`) — fill them with real uploads, not guesses.
- **vault host selection (2.12.0, 2026-08-30)**: the vault blob target, move-to-vault source, and restore target are now user-selectable (was hardcoded UDrop). `selectedVaultHost` (chrome.storage.local, default `'udrop'`, `'udrop'|'terabox'`) = where the encrypted `.bin` lives; stored as `vaultHost` on the vaulted item (round-trips via extra_metadata, no schema change). `encryptAndUploadVaultedBlob(blob, meta, onProgress, vaultHost)` dispatches to UDropUploader or TeraBoxUploader (fetch-based, SW-safe). `decryptVaultBlob`/`resolveVaultDownloadUrl(url, fileId, vaultHost, fileName)` regenerate the correct host's download URL (udrop `/file/download` vs terabox `resolveTeraBoxPlaybackUrl` dlink). `moveItemToVault(id, {sourceHost, vaultHost})` uses the STRICT source-host link (`getStrictVideoProviderLink`/`getStrictImageProviderLink`) — no hardcoded fallback chain; throws if the picked host has no link. `restoreFromVault(id, {targetHostKeys})` filters restore targets by the picker (defaults to all configured when omitted). UI: GalleryPage upload modal shows a UDrop/TeraBox vault blob host picker when `uploadToVault`; new `VaultMoveModal` component (source host + blob host) for single/bulk move-to-vault; VaultPage restore shows an `UploadHostSelector` (`selectedRestoreHostKeys`). legacy vaulted items without `vaultHost` decrypt as udrop. **2.12.4/2.12.5 catalog-driven vault hosts**: never hardcode the vault blob host list/labels in UI or dispatch code — they come from `providerCatalog.js`. `getVaultBlobHostServices()` returns ALL video services (2.12.5 dropped the `vaultBlobHost` filter), so every video host is selectable as the encrypted-blob target and shows up in the VaultMoveModal + GalleryPage pickers automatically; `getVaultBlobHostOptions()` gives the picker list; `DEFAULT_VAULT_BLOB_HOST` is the storage default. each service supplies `uploaderClass` (page-side construction) and optional `vaultDownloadUrl` (fresh download URL regen — absent = falls back to the stored URL). background `encryptAndUploadVaultedBlob`/`resolveVaultDownloadUrl`/GalleryPage direct vault upload all dispatch through the catalog service by key — no host-name branches. add a new host by adding a VIDEO_UPLOAD_SERVICES entry with `uploaderClass` (+ `vaultDownloadUrl` when it needs fresh-URL regen) — no other code changes.
- **vault restore bug (2.12.1, 2026-08-30)**: two-part bug where "Restore to Gallery" showed instantly but the item stayed in the vault. (1) `this.teraboxUploader` was NEVER instantiated in the background constructor (only pixvid/imgbb/filemoon/udrop were), so the restore loop `this[service.uploaderKey]` found `undefined` for terabox, silently skipped it, ended with empty upload results, and threw → fell back to legacy flag-only restore (no actual upload). fixed: `this.teraboxUploader = new TeraBoxUploader()` in the constructor. (2) even the legacy flag-only restore couldn't clear `isVaulted`: `buildExtraSeed` in `shared/mediaDbPayload.js` (and its vendored copy `web/src/shared/mediaDbPayload.js`) spread `sourceExtra` (the stale DB extra_metadata with `isVaulted: true`) LAST, clobbering the explicit `isVaulted: false` update. fixed: only carry over extra-only keys not already present at top level (`extraOnly` filter), so explicit top-level updates win. both files must stay in sync.
- **NEVER `await import()` a chunk from the MV3 service worker (2.12.3, 2026-08-30)**: Vite's dynamic-import runtime (`dist/assets/preload-helper-*.js`) calls `document`/`window` APIs; in a SW neither exists, so a failed or even a *successful* dynamic import that hits the preload-error path throws `ReferenceError: window is not defined` — the restore path got this as "Restore failed: Vault restore failed: Vault restore failed: window is not defined". the encrypt/decrypt vault functions were split into `vaultCrypto-*.js` because `decryptBlob`/`decryptMetadata` were imported with `await import('../utils/vaultCrypto.js')` inside `decryptVaultBlob` and `restoreFromVault`. fix: statically import ALL vaultCrypto functions in background.js (decryptBlob/decryptMetadata were missing from the static import that already had encryptBlob/encryptMetadata), and remove the two dynamic imports. static imports bundle into the SW module graph and always work; dynamic imports route through the window-reliant preload helper. same applies to any other runtime `import()` in background.js (sceneFileCache.js still uses it — scene save/load will hit the same bug; convert to static import if that flow matters).
- **vault restore silent half-restore bug (2.12.2, 2026-08-30)**: "Restore to Gallery" showed success but the item had NO playable URL — symptom: DB row with `isVaulted:false` in extra_metadata but `encryptedBlobUrl`/`encryptedMetadata`/`encryptedBlobFileId` still present and all provider URL columns empty, file_name still `*.bin`. root cause: MV3 SW idle-restart wipes the in-memory `this.vaultMasterKey` (background.js constructor resets it to null); the page's session-restore only checked `sessionStorage.passHash === config.passHash` and set `unlocked=true` WITHOUT re-sending the key to the SW, so `restoreFromVault`'s `if (current.encryptedBlobUrl && this.vaultMasterKey)` was false and it SILENTLY fell into the legacy flag-only restore (just flips isVaulted, no decrypt/re-upload). fixed two ways: (1) VaultPage `unlockWithPasscode` now stores `keyB64` in sessionStorage alongside passHash, and the session-restore block rehydrates both page (`importMasterKeyFromB64`) and SW (`vaultSetMasterKey`) keys when the session is valid; (2) `restoreFromVault` now FAILS LOUDLY (`throw new Error('Vault is locked. Unlock the vault before restoring this encrypted item.')`) when `current.encryptedBlobUrl` exists but the SW key is missing — it can no longer silently legacy-restore an encrypted item. repair path for a half-restored item: set it back to vaulted in the DB (`extra_metadata = extra_metadata || '{"isVaulted": true, "vaultMode": "hidden", "vaultedAt": "<now>"}'::jsonb`) then restore it again with the vault unlocked. sessionStorage holds the raw keyB64 tab-scoped — acceptable (same trust as passHash), cleared on tab close / lockVault.
- **vault streaming — TeraBox dlink 403 + slow resolve (2.12.10, 2026-08-31)**: TeraBox-hosted vault items stalled ("player shows but loads forever") after `request for ... range: bytes=0-` with no further log. two root causes. (1) **the SW's dlink range fetch sends NO terabox cookie** — `dm-d.terabox.com/file/...` dlinks 403 without the session cookie (the gallery works because a `<video src>` lets the BROWSER attach cookies; a bare `fetch(url, {Range})` from the SW does not). fix: `fetchVaultBlobRange` now calls `getTeraBoxRangeCookie()` (settings `teraboxCookie` override, else live `chrome.cookies.getAll({domain:'terabox.com'})`, cached 10min) and sends `Cookie` + `Referer: https://www.terabox.com/` + Chrome UA on terabox copies. (2) **`getFreshVaultStreamUrl` can block for 30s+** — `resolveTeraBoxPlaybackUrl` scrapes a jsToken via a hidden tab (up to ~35s) then paginates the ENTIRE `/` root folder to find the fs_id before `/api/filemetas`, and `resolveVaultDownloadUrl` returns the stored URL on failure so the resolve is never cached and re-runs per request. fix: wrap the resolve in an 8s `Promise.race` timeout in `getFreshVaultStreamUrl` so it fails loudly (`fresh URL resolve timed out`) instead of stalling the player. the fetch-listener 30s timeout now returns a real `504` Response instead of rejecting (a rejected `respondWith` makes the video element retry indefinitely → "loading forever").
- symptom reads: `[Vault-stream] fetch intercepted` + `request for ... range:` logged but nothing after = stuck in the fresh-URL resolve (slow terabox jsToken/folder-list chain); `dm-d.terabox.com/file/... 403` in the network log = dlink fetched without cookie/referer. a loud `header fetch failed: HTTP 403` or `Vault stream timed out` after 8s/30s is the correct loud behavior when the cookie is missing.
- **vault stream page-resolve data shape gotcha (2.12.12, 2026-08-31)**: `useChromeMessage()`'s `sendMessage` already unwraps `response.data` on success (resolves `response.data`), so a page handler must read the payload directly, NOT `res.data.xxx`. VaultPage `ensureStreamResolved` was checking `res.data?.copies` (always undefined → threw `Failed to resolve stream link` even when the SW resolve succeeded); fixed to read `res.copies`. same trap applies to every sendMessage call site in pages.
- **vault streaming — page-side pre-resolve + fresh flag (2.12.11, 2026-08-31)**: the 2.12.10 8s timeout on getFreshVaultStreamUrl made the terabox resolve fail loudly (good) but the player still got MEDIA_ELEMENT_ERROR: Format error because the SW hot-path resolve timed out. root cause: terabox's `resolveTeraBoxPlaybackUrl` runs a hidden-tab jsToken scrape (up to 35s) + full `/` folder pagination, which exceeds the 8s per-range budget, and the SW's `enter` event doesn't let it retry persistently. fix: **move the resolve out of the SW hot path entirely**. VaultPage now calls a new SW message `vaultResolveStreamUrls` BEFORE the video mounts (with a 30s budget, visible "Resolving stream link…" spinner), which resolves fresh dlinks for every copy, caches them in `vaultStreamUrlCache`, and returns copies marked `fresh: true`. Back in the SW: `getFreshVaultStreamUrl` returns a fresh copy's URL immediately (no re-resolve — `if (copy.fresh && copy.encryptedBlobUrl) return copy.encryptedBlobUrl`); `handleVaultStreamRequest` preserves the `fresh` flag from query params. The result: terabox resolve runs once, visible, and the SW hot path is always fast. also the `vaultResolveStreamUrls` message handler (`resolveVaultStreamUrls` method) loops each copy with a 30s per-copy `Promise.race` timeout, caching the result via `vaultStreamUrlCache.set(`${id}:${host}`, url)`. page state: `streamResolving` (itemId → bool), `freshStreamCopies` (itemId → array of fresh:true copies), `getVaultStreamUrl(item)` uses `freshStreamCopies[item.id]` when available, falling back to stored copies. renderVaultMedia shows the spinner during resolve and a loud error on failure (AlertTriangle + error message).
- **vault streaming (2.12.8/2.12.9, 2026-08-31)**: vaulted video previews no longer download+decrypt the whole blob — the SW serves the DECRYPTED media over HTTP Range so playback/seek needs only the covering chunks. blob layout = 16-byte header `"IVG1"` + uint64le(plaintext total) + uint32le(chunkSize, 8MiB), then per chunk 12-byte IV + AES-256-GCM ciphertext(+16 tag); chunk `i` encrypted offset = `16 + i*(12+chunkSize+16)`. helpers in vaultCrypto.js: `parseVaultBlobHeader`, `getVaultChunkLayout`, `decryptEncryptedChunk`. background `handleVaultStreamRequest(request, itemId)` (registered in the SW `fetch` listener under `^/vault-stream/([^/]+)$`) reads the 16-byte header via a ranged fetch, returns 206 + Content-Range for Range requests (or the whole body for non-Range), and streams `Uint8Array`s through a ReadableStream, decrypting only covering chunks. **2.12.9 — NO Neon DB read per Range request**: `handleVaultStreamRequest` must NEVER call `getImageById(itemId)` — a video player issues ~1 range request per second and each DB round-trip stalls playback ("player shows but loads forever"). the SW builds the item context from URL query params instead: VaultPage `getVaultStreamUrl(item)` embeds `copies` (JSON array of `{host, encryptedBlobUrl, encryptedBlobFileId}`), `mime`, `name` in the stream URL; the handler falls back to the DB item ONLY when `copies` is absent (legacy URLs). also add a 30s overall `Promise.race` timeout around `handleVaultStreamRequest` in the fetch listener and a 30s per-chunk timeout around each `fetchVaultBlobRange` so a stalled host fails loudly instead of hanging the player. udrop/terabox both honor Range (`Accept-Ranges: bytes`, 206) — streaming depends on it. when the stored `encryptedBlobUrl` is the udrop WATCH url (serves HTML at 200), range reads must resolve a fresh signed download URL first (`getFreshVaultStreamUrl`, cached per `${item.id}:${host}`); the DB item still needs `vaultStreamUrlCache` so a freshly uploaded vaulted item streams without restarting the SW.
- **vault-stream terabox 403 + format error root cause (2.12.15, 2026-09-01)**: the 2.12.10-2.12.14 chain fixed cookies + pre-resolve but the video STILL died with `MEDIA_ELEMENT_ERROR: Format error` after `[Vault-stream] layout ok` + `chunk 0 fetch 16 8388636` and 4× `dm-d.terabox.com/file/... 403` in the network log. root cause: **terabox dm-d dlna dlinks are SINGLE-USE** — each signed URL serves ONE request, so the header read (range 0-15) consumes the pre-resolved fresh dlink and the chunk read (range 16-8388651, the whole 8MiB chunk) of the SAME URL 403s. the `fresh`-flag shortcut in `getFreshVaultStreamUrl` (`if (copy.fresh && copy.encryptedBlobUrl) return copy.encryptedBlobUrl`) made the retry in `fetchVaultBlobRange` re-fetch the exact same consumed URL, so every copy failed. ALSO: terabox CDN throttles to ~30KB/s (plan-terabox-vault.md), so an 8MiB encrypted chunk needs ~280s — the 30s per-chunk and 30s outer fetch-listener timeouts would kill even a *working* stream. fix (2.12.15): `getFreshVaultStreamUrl` gained a `forceResolve` option; `fetchVaultBlobRange` now passes `{ forceResolve: true }` for terabox copies so EVERY byte-range read (header + each chunk) resolves a genuinely fresh signed URL via `resolveTeraBoxPlaybackUrl` instead of reusing the consumed one. chunk timeout is host-aware: terabox = 15min (30KB/s × 8MiB ≈ 280s + resolve margin), udrop = 30s. outer fetch-listener race is host-aware: terabox = 20min, others = 5min (300s, generous enough for any slow host without hiding a truly stalled handler). `resolveTeraBoxPlaybackUrl` now caches `fs_id → path` in a module-level `_teraBoxFsPathCache` (teraBoxApi.js) so the per-chunk fresh resolve skips the slow full-root-folder pagination (which was the other 30s+ sink) and hits `/api/filemetas` directly — the vault blob file doesn't move, so the path is stable for the SW session. NOTE: even with the fix, terabox vault streaming is SLOW (30KB/s CDN cap) — a 1GB blob ≈ 9h to stream. if the fix regresses, check the `isTeraBox` detection: `handleVaultStreamRequest` sets `chunkTimeoutMs = isTeraBox ? 15min : 30s` from the `copies` query param, and the outer listener race parses the same `copies` param — both must agree on what counts as a terabox copy (host lowercased === 'terabox').
- **vault-stream terabox SPEED root cause (2.12.16, 2026-09-01)**: even with forceResolve, terabox vault streaming was still stuck at ~30KB/s (1MB range took 71.6s, measured with curl). the user correctly reported "gallery terabox items is more faster" and "make it like the rclone alist". investigation: measured the same 1.3GB blob through AList+rclone → **0.68 MB/s sustained** (60s test, single connection via `rclone cat --multi-thread-streams 1`). compared direct dlna dlink fetch with cookie vs without: dlink redirect WITHOUT cookie → `tsl=2000&csl=2000` in the CDN URL, 0.68MB/s download; WITH cookie → `tsl=30&csl=30`, 0.01MB/s. **the CDN throttle is triggered by the logged-in account cookie.** the 2.12.10 "cookie fix" (adding Cookie+Referer to dlink range reads) was backwards — it made the CDN identify the request as the free-tier account and apply the 30KB/s cap. the gallery plays fast because the browser's `<video>` in the extension page is cross-origin to terabox.com, so SameSite/Lax drops the cookie → tsl=2000. fix (2.12.16): removed the Cookie header from dlink range reads in `fetchVaultBlobRange` for terabox copies. also removed the now-dead `getTeraBoxRangeCookie` method. the dlink itself is a signed CDN URL that works without cookies (tested: 8MiB range read, no cookie, 0.68MB/s, works for repeated reads on the same dlink). forceResolve (fresh dlink per chunk) is retained as a safety net but the speed win is entirely from dropping the cookie. keep `User-Agent` and `Referer` headers — they don't trigger throttle.

- **CRITICAL: credentials omit for terabox dlink fetch (2.12.17, 2026-09-01)**: removing the explicit Cookie header (2.12.16) was NOT enough - Chrome SW with <all_urls> auto-attaches site cookies to fetch() even without explicit Cookie header, so the CDN still saw the session cookie -> tsl=30. the real fix: credentials:'omit' on the dlink fetch(url, { headers, credentials: 'omit' }). this forces Chrome to send NO credentials regardless of host permissions. apply to BOTH the primary and retry fetch in fetchVaultBlobRange. the gallery <video> gets no cookie because extension page is cross-origin to terabox.com (SameSite=Lax drops it) -> tsl=2000. never send the cookie explicitly - that triggers the throttle.

- **vault modal autoplay (2.12.18, 2026-09-01)**: the shared MediaDetailModal takes a per-page renderMedia prop. GalleryPage's renderModalMedia had autoPlay on <video>; VaultPage's renderVaultMedia did not -> gallery autoplayed, vault didn't. fix: added autoPlay + key={item.id} to both vault video elements (encrypted stream path and plain directUrl path). the key forces remount on item change so each new item autoplays. the 2.12.13/14 diagnostic logs (terabox dlink fetch: / terabox fetch done:) in fetchVaultBlobRange pinpoint where the stream stalls - keep them.

- **media modal overview: skip raw imageHosts/videoHosts blobs (2.12.19, 2026-09-01)**: the shared modal Overview tab showed the raw nested host JSON (imageHosts / ideoHosts — e.g. 6. imageHosts {"pixvid":{...},"imgbb":{...}}) as one giant field. NOT intended — getOverviewEntries (in BOTH shared/mediaFieldRegistry.js and the vendored copy web/src/shared/mediaFieldRegistry.js, keep them in sync) already expands the nested hosts into individual per-provider rows (pixvid.url, imgbb.url, pixvid.deleteUrl, ...) via the getNestedHosts loop, so the raw blob was redundant leftover. fix: skip imageHosts/ideoHosts in the base-key loop of getOverviewEntries (if (key === 'imageHosts' || key === 'videoHosts') continue;). only the flattened rows show now. reminder: every shared/ edit must be mirrored into web/src/shared/ and then pnpm build + start msedge http://reload.extensions (the extension loads from dist/, not source — forgetting the rebuild makes the old UI persist).

- **native download graceful stop (2.12.20, 2026-09-01)**: stopping a 24 GiB yt-dlp download flooded the log with 4× `Error when communicating with the native messaging host.` Root causes: (1) **Chrome 1 MB native-message limit** — `download_video_with_progress` returned the full yt-dlp stdout/stderr (hundreds of KB of progress spam) in its final `NativeResponse`; Chrome silently disconnects the port when the JSON exceeds 1 MB, surfacing as the generic host error. (2) **Race on cancellation status** — `cancelNativeDownload` wrote `cancelling` to `chrome.storage.local` async, but the live download port's `onMessage`/`onDisconnect` read it async too; the kill (via a second `connectNative` `cancel_download` that `taskkill`s the yt-dlp PID) could land before the `cancelling` flag was visible, so the termination was treated as a failure and logged as a host error. (3) **Host kill left a large error payload** — the blocked download thread's `Err(DownloadOutcome)` included the full log again. Fix: Rust — `truncate_for_native_message(text, 40_000)` (UTF-8-safe, prefers newline) on final stdout/stderr; `get_cancel_flag_path`/`write_cancel_flag` temp file so the blocked thread can distinguish user-cancel (returns `Err("Download stopped by user")` with 8 KB truncated logs) from a real failure; `cancel_download_request` writes the flag before `taskkill`, gracefully tries `taskkill /T` without `/F` first then force-kills, and treats missing PID / already-finished as success to avoid scary errors; `delete_download_journal` also cleans the flag; `kill_process_tree` now graceful-then-force. Background — `cancellingNativeDownloads` `Set` gives synchronous in-memory cancellation visibility, `appendNativeDownloadLog` no longer settles on `stop signal sent` (only on `download stopped` / `yt-dlp failed`), `cancelNativeDownload` only logs `Stop signal sent...` and keeps status `cancelling` until the download port settles (12 s fallback to `cancelled`), host-gone during cancel is treated as success.

- **64MiB sendMessage limit + 3D switch (2.12.21, 2026-09-08)**: `runtime.sendMessage` caps at 64MiB — the Gallery upload modal sent `fileBlob` (or SceneUploadDialog sent `spzArray` as plain numbers) through the message channel; a 70MB file expands to ~210MB JSON and throws `Message exceeded maximum allowed size of 64MiB` on Cancel/upload. Fix: (1) never send the blob via message for large files — `uploadPreparedMedia` now checks `blobSize > 60MiB` and routes through page-side XHR (`upload3DDirectly`/`uploadVideoDirectly`) which bypasses the limit, and `createPendingUpload` strips `fileBlob` before `sendMessage`. (2) SceneUploadDialog guards `totalBytes > 20MiB` with a loud alert directing to the new Gallery 3D switch. (3) Gallery default upload modal now has a 2D/3D toggle (motion `Box` icon, cyan when ON, animated in header/dropzone/preview) — `is3DMode` state, `is3DUploadFile` regex (`glb|gltf|obj|fbx|stl|spz|3mf|usdz|usd|blend|dae|ply|abc|bvh`), `configuredUploadServices` filters to `udrop|terabox` when ON, `accept` swaps to model extensions, dropzone text/preview/title animate, and `upload3DDirectly` does direct XHR to UDrop/TeraBox (no 64MiB, with progress logs) then `finalizeUploadedVideo`/`updateImage`. as success, `handleNativeDownload` checks `isMemoryCancelled || isStatusCancelled || /Download stopped by user/` and cleans the Set, `onDisconnect` swallows the Chrome 1 MB disconnect when cancelling. HostPage — `handleNativeDownload` and `handleStopNativeDownload` map `Download stopped` / `cancelled` to `warning` not `error`, and suppress the Chrome disconnect string.
- **vault/video vs 3D routing bug (2.12.70, 2026-09-10)**: `uploadPreparedMedia` checked `isLargeForMessage (>60MiB) || is3D` BEFORE `isVaulted`/`isVideo` — a 675 MB `.mp4` video (reported as `UDrop 3D progress: 5% (33 MB / 675 MB)` + `3D payload ready: 675 MB (5idmd [5idmd].mp4)`) was forced into `upload3DDirectly` via the generic `fileBlob -> is3D:true` fallback, so the Live Upload Log showed `UDrop 3D progress` for a normal video and the heading said `Uploading video to TeraBox...` while UDrop 3D ran. root cause: `buildUploadData` also forced `is3D=true` whenever `is3DMode` was ON (`Boolean(is3DMode || mediaData.is3D || is3DUploadFile(file))`) — non-3D files became 3D when the toggle was left on. fix: vault wins first (`isVaulted -> uploadVaultedDirectly`), then explicit 3D (`is3D -> upload3DDirectly`), then video (`isVideo && fileBlob -> uploadVideoDirectly`); the 60MiB `isLargeForMessage` fallback now only runs for images/generic files after those three. `buildUploadData.is3D` no longer reads `is3DMode` — it is `Boolean(mediaData.is3D || is3DUploadFile(file))`; `is3DMode` only filters `configuredUploadServices` (UDrop/TeraBox when ON), not classification.
- **vault unlock not shared across pages (2.12.71, 2026-09-11)**: `Encrypted vault upload failed: Secret Vault is locked. Unlock it before saving encrypted items.` even though the vault was already unlocked on the Vault page. cause: `vaultSession.js` holds the master key in **per-document** memory — `vault.html` (VaultPage) and `gallery.html` (GalleryPage) are separate extension pages, each with its own copy; unlocking in one never fills the other. the gate `handleUploadSubmit` checks the SW's `getVaultStatus.unlocked` (shared) so it passes, then `uploadVaultedDirectly` reads the **gallery page's** empty copy and throws. fix: the SW is the shared source of truth — new `vaultGetMasterKey` message (`getVaultMasterKeyB64` exports the raw key while unlocked, null when locked); GalleryPage `ensureVaultKeyHydrated()` fetches it and `importMasterKeyFromB64` into the page session before encrypting, so unlocking once (Vault page or the upload lock screen) covers every page. VaultPage unlock already pushes the key to the SW (`vaultSetMasterKey`), so the SW copy exists for the gallery to pull from.
- **yt-dlp "did not return a file path" false error (native host, 2.12.72, 2026-09-12)**: downloads finished fine (log showed the final `...\[id].mp4` path streamed at `[yt-dlp]`), but the extension then logged `yt-dlp did not return a file path` (twice) and never auto-uploaded. cause: in `download_video_with_progress` the host head-truncates `stdout_text`/`stderr_text` to 40 KB via `truncate_for_native_message` (to stay under Chrome's 1 MB native-message cap), and THAT truncation ran BEFORE the path parse — but `--print after_move:filepath` writes the path as the very LAST line, so `--verbose` progress spam pushed it past the 40 KB cut and it got deleted; the parse also only scanned `stdout` (the path was effectively gone) → `None` → the false error. fix: added `looks_like_downloaded_path()` + `extract_downloaded_path(stdout, stderr)` (absolute-path heuristic: `C:\`/`C:/`/`\\UNC`/`/` + real extension, rejects `[..]`/`WARNING:`/`ERROR:`/URLs/ffmpeg `Input #0 … from '…'` log lines), scan stdout then stderr, and call it on the FULL streams BEFORE truncation. same extractor now used by the non-progress `download_video` (was: naive "last non-empty stdout line", which could grab a WARNING and had no stderr fallback). rebuild the exe (see next gotcha) — no extension/manifest change.
- **cargo `-gnu` build gotcha — "only metadata stub found for rlib dependency core/object"** (native host, 2026-09-12): after `cargo clean`, the first `cargo build --release --target x86_64-pc-windows-gnu` failed compiling memchr/itoa/winapi build-scripts with a "metadata stub for core" + "cannot resolve a prelude import" + "`println!` not in scope" cascade. this is NOT a broken toolchain (verified `libcore`/`libstd` full-size and a plain `rustc --edition 2021 --target x86_64-pc-windows-gnu smoke.rs` succeeds) and NOT AV quarantine of the sysroot — it's a transient race where freshly-written `.rmeta`/`.rlib` in the scoop-persisted `target/` were re-read before their full metadata landed (folder being scanned/synced). the fix is just to RE-RUN the build: the already-compiled units go `Fresh` and the next pass finishes clean (~23s). don't reinstall rust-std / the toolchain for this. rebuild = `cargo clean` only when the `target/` cache itself is corrupt; otherwise a retry clears it. then copy `target\x86_64-pc-windows-gnu\release\imgvault-native-host.exe` over `native-host\ImgVault-Native-Host.exe`.
- **extension flips back OFF on reload — missing `dist/scene-viewer.js` (2.12.73, 2026-09-12)**: after a clean `pnpm build` (vite `emptyOutDir` wipes dist), enabling ImgVault at `edge://extensions` reverted to off on refresh; `Secure Preferences` showed the extension with `state: (null)`, `disable_reasons: 4` (= `DISABLE_CORRUPTED`) and `service_worker_registration: False`. root cause: the manifest `web_accessible_resources` declares a CONCRETE `scene-viewer.js`, but NOTHING produced it into `dist` — it lives at `src/scene-viewer.js` (never in `public/`, never in git), and `postbuild.ps1` only copied the css/contextmenu/icons while vite only emits the hashed `assets/sceneViewer-*.js` for the `scene-viewer.html` entry. so a wiped `dist` had no `dist/scene-viewer.js`; Chromium treats a declared-but-absent web-accessible resource as a load error and Terminate+disables the unpacked extension, and the self-reload cannot clear a persisted corrupt flag. the trigger that removed the stale file this time was the first (ENOMEM-failed) `vite build` that emptied dist and never completed the copy set. fix: `postbuild.ps1` now `Copy-Item src/scene-viewer.js dist/scene-viewer.js` every build (verify with the loop that checks EVERY concrete manifest-referenced path — bg service_worker, action/content/icons paths, non-glob web_accessible_resources — exists in dist; `lib/*.js` is a glob, tolerant of zero matches). durable guard: `tools/reload-extension.ps1` now captures `$LASTEXITCODE` from `pnpm build` and FAILS LOUD without opening `reload.html` on a non-zero build (a partial dist self-reloaded = exactly this corruption; never reload on a failed build). recovery once corrupt: complete `dist` + click the extension's Reload (circular arrow) in `edge://extensions` (dev mode) — the flag only clears on a successful re-registration; toggling alone won't.
- **vault video grid previews (2.12.74, 2026-09-13)**: encrypted vault videos showed a bare LockKeyhole tile (images keep that by request — untouched). nothing is stored per-item and OLD videos work: `src/utils/vaultPreview.js` derives a preview AFTER unlock by mounting a detached `<video>` on the existing `vault-stream/<id>` SW range endpoint, seeking to the **middle** (candidates 50% → 25% → 75%), and scoring the frame on a 48px canvas (Rec.601 mean luma ≥18 AND stddev ≥8; industry practice — frame 0 is usually black), keeping the most-detailed frame if all look dark; the final 480px JPEG lands in the shared IndexedDB thumbCache (`vault-preview-<id>`, 7-day TTL) so every video pays its chunk fetches once per machine. terabox guardrails: per-host concurrency queue (terabox=1 — CDN throttles the whole account ~0.68MB/s; udrop=3), generous host-aware timeouts (metadata 180s/seek 150s terabox), IntersectionObserver (+200px) so only visible cards queue, and a **Warm previews** button on the unlocked grid that serializes the whole library with `n/total · failed` progress (click again = stop). VaultPage passes `getPreviewStreamUrl` which runs the page-side `vaultResolveStreamUrls` pre-resolve FIRST (terabox dlink resolve is 30s+ and must never hit the SW hot path) and feeds the fresh copies straight into `getVaultStreamUrl(item, copiesOverride)` — the state setter lags, so never read `freshStreamCopies` right after resolving. classification prerequisite: `getMediaItemKind` (shared/mediaFieldRegistry.js + vendored web copy) had NO way to see encrypted videos are videos — the mime lives in `encryptedMimeType` (fileType is `video`/`image`, file_name `*.bin`) and the isVideo flag is inside `encryptedMetadata` → every encrypted item classified as Image. fixed with `_decryptedMeta.isVideo` + `encryptedMimeType.startsWith('video/')` checks in BOTH copies (the old dead `item?.mimeType || item?.mime_type` truthy line is gone; the web copy never had it). loud per-item on failure: `[VaultPreview]` console warn + card keeps the lock tile with a "preview unavailable" label — never a fake poster. **2.12.75 regression fix — previews broke detail-modal playback**: a derivation fires range reads while the modal's player is also reading the same item → concurrent terabox dlink resolves (hidden-tab jsToken scrape) trip the token gate, the fallback reuses the CONSUMED single-use dlink → 403 → stream dies; and the extractor mounting a `<video>` on a LEGACY (non-IVG1) blob hits the handler's whole-file decrypt branch → SW memory death → `vaultMasterKey` lost → 403 on every vault stream. Three guards now: (1) `setVaultPreviewPaused(true)` while `selectedItem` is set (blocks thumb starts, already-queued jobs via pumpHost, and the warm loop waits on `modalOpenRef`), (2) `getPreviewStreamUrl` runs `vaultProbeBlobFormat` first and LOUDLY skips legacy blobs, (3) ALL terabox `resolveVaultDownloadUrl` calls go through the SW `_enqueueTeraBoxResolve` promise-chain mutex (getFreshVaultStreamUrl + resolveVaultStreamUrls; terabox forceResolve budget raised to 60s for the queue wait). Going forward: anything new that touches `vault-stream/` must respect the pause gate + resolve mutex. **2.12.76 speed-up**: cached previews NEVER re-derive (card checks `getCachedVaultPreview` → memory `objectUrlMap` → IndexedDB before touching the queue; warm loop pre-filters cached) — the only churn was the shared 7-day thumb TTL, so previews now carry their own 365-day TTL (`getCachedThumb(key, maxAgeMs)` gained the override; vault blobs are immutable so a derived frame never goes stale); derivation prefers the FAST copy — `preferredPreviewCopies` picks the non-terabox host when the item has one (udrop: seconds; same IVG1 bytes on every host) and `getPreviewStreamUrl` resolves ONLY those copies via `vaultResolveStreamUrls` (no longer via `ensureStreamResolved`, which paid slow terabox dlink resolves for reads never done and cached consumed links into the modal state); terabox queue concurrency 1→3 (the ~0.68MB/s cap is PER dlink connection — rclone multistream evidence). No fallback: if the chosen fast copy fails, that preview fails loudly.

- **vault trash restore provenance fix (2.12.82, 2026-09-14)**: `restoreFromTrash*` only re-vaulted rows with `encryptedBlobUrl`, but flag-only vault items can have `isVaulted`/`vaultMode` without an encrypted blob. Those rows returned to the gallery after trash restore, so users said restored items were missing from the vault. Fix: vault delete now clears only `isVaulted`, preserves `vaultMode`/`vaultedAt`, and writes explicit `extra_metadata.wasVaulted:true`; `moveToVault`/encrypted upload paths clear `wasVaulted`; restore re-vaults when `encryptedBlobUrl || wasVaulted || vaultMode:hidden || vaultedAt` is present and then clears `wasVaulted`. Registry whitelist updated in both shared/mediaFieldRegistry.js and web/src/shared/mediaFieldRegistry.js. Existing trashed rows that pre-date the marker were repaired with SQL: `isVaulted=false`, `wasVaulted=true`, preserving `vaultMode`/`vaultedAt`. Also fixed stale Vault tab state by broadcasting membership changes via `chrome.storage.session.imgvaultVaultMembershipChangedAt`; VaultPage listens while unlocked and reloads after move/restore/delete/trash restore.
- **vault previews persisted server-side, encrypted (2.12.83, 2026-09-18)**: derived previews used to live ONLY in per-machine IndexedDB (ault-preview-<id>, 365-day TTL), so a clean install or a new machine re-derived every preview (each = an 8MiB chunk fetch through a throttled terabox CDN). New server-side tier, deliberately NOT in the media_items row: new table public.media_item_previews (item_id text pk references media_items(id) on delete cascade, preview_encrypted text, created_at) holds the SAME 480px JPEG encrypted client-side with the vault master key (single-shot AES-GCM base64 iv||ct||tag, ncryptPreviewBytes/decryptPreviewBytes in vaultCrypto.js - Neon only ever stores ciphertext, the key never leaves the client, and the value is useless without the passcode). Bandwidth rule: getVaultImages NEVER joins this table - vault-page payloads stay byte-identical to pre-2.12.83; the card's IntersectionObserver path fetches its own row lazily through the SW (getVaultPreview/saveVaultPreview/deleteVaultPreview messages), so a cache miss costs ~60KB instead of 8MiB. storage.js has neon + firestore variants (firestore stores the same base64 in the item doc field ncryptedPreview). Serve order in unExtraction: in-memory objectUrlMap -> IndexedDB -> remote row (decrypt; backfills IndexedDB so later views stay free) -> derive. A decrypt failure is LOUD (stored preview unusable ... re-deriving) and the derivation overwrites the bad row - self-healing, never a fake frame. Persist after derivation is best-effort (card already holds the local copy; a failed write warns, never breaks the tile). Cleanup: restore-to-gallery deletes the row (SW estoreFromVault case) + local deleteCachedThumb (VaultPage); trash-restore that does NOT re-vault deletes the row too (estoreFromTrashNeon); soft-delete to trash KEEPS it (the blob survives a re-vault, preview stays valid); permanentlyDeleteNeon's hard delete is covered by the FK on delete cascade. VaultPage threads sendMessage into equestVaultPreview (the module is page-context, the hook can't run there). Live DB migrated with the exact create table if not exists; src/db/schema.ts mirrors it (schema.ts changes REQUIRE the live migration - repo schema is not auto-applied, 2.12.81).
- **vault preview remote backfill (2.12.84, 2026-09-18)**: 2.12.83 shipped with the remote persist only inside unExtraction, which RETURNS on a local cache hit — so on any machine with a warm IndexedDB (365-day TTL) no derivation ever runs and media_item_previews stayed at 0 rows until the local TTL expired. Diagnosed by select count(*) from public.media_item_previews = 0 right after a live deploy. Fix: getCachedVaultPreview(item, sendMessage) now backfills on a local hit — one getVaultPreview check per item per session (emoteBackfilled Set; flag RELEASED on failure so a transient network blip retries next session, never silently permanent), persists if the row is absent, fire-and-forget (the card already holds its URL; this only feeds other machines). Both call sites pass sendMessage (the card effect + the warm loop's cached-filter). Serve order is unchanged: memory -> IndexedDB -> remote row -> derive; the backfill only adds a write on the path that used to no-op.
- **terabox-only vault previews die on moov-probe churn — small-blob plaintext cache (2.12.85, 2026-09-18)**: two vault items (9cc25ea7 / 086ea473, both 23MiB, BOTH terabox-only - no udrop fast copy) timed out at preview metadata (terabox) timed out after 180s. The terabox chain was NOT broken - SW logs showed layout ok, chunk 0 fetch, 	erabox fetch done: 206 bytes 16-8388651/24403594, then stream error: Cannot enqueue a chunk into a closed readable stream. Root cause: Chrome's media engine with preload: 'metadata' PROBES for the MP4 moov atom by opening range requests (ytes=6062080- is a mid-file probe) and ABORTING them; every abort closes the ReadableStream and the next probe starts a fresh ault-stream request = fresh header read + fresh serialized terabox dlink resolves (60s budget each through the single _enqueueTeraBoxResolve mutex). The probe sequence churned faster than the serialized resolves could feed it, so loadedmetadata never landed. udrop items never hit it (instant resolves). Fix: after layout ok, when 	otal <= 64MiB decrypt the WHOLE blob once (_decryptFullVaultBlob: one range fetch of the full encrypted span + per-chunk decrypt into one Uint8Array) and cache the plaintext in an LRU (aultPlaintextCache, max 3 items) keyed by itemId; every range request then slices memory - probes are free, zero host reads after the first. Multi-GB blobs keep the chunk-streaming path (a full decrypt of those killed the worker before, 2.12.75). ALSO fixed the noisy symptom: nqueue/rror on a closed controller threw and left a broken response behind - the stream's start now checks controller.desiredSize !== null (live()) before every enqueue/error/close and stops cleanly on abort. The cache is cleared on vault lock (aultClearMasterKey + setVaultMasterKey('')): plaintext must never outlive the key. Symptom-to-cause map: stream error: ... closed readable stream = client aborted the response (probe moved on) - with the cache it stops happening for small blobs; preview metadata timed out on a terabox-only item = the probe churn, fixed here; header fetch failed / 502 = host/resolve genuinely down.
- **vault stream chunk-granule cache for big blobs (2.12.86, 2026-09-18)**: after 2.12.85 the preview count went 32 -> 33 of 34.  86ea473 (23MiB) decrypted via the whole-blob cache and derived; 9cc25ea7 was still timing out because it is **143.7MiB** - 2.25x over the 64MiB whole-blob cap, so it kept the chunk-streaming path and kept dying on the moov-probe churn. Raising the cap is unsafe (143MiB Uint8Array x LRU in the worker). Fix: a per-item DECRYPTED-CHUNK granule cache on the streaming path - before fetching chunk i, check aultChunkCache[\item:i\]; after decrypting, store it. 8MiB granules, LRU, global byte cap 128MiB (~16 chunks) - enough for a moov probe's working set (chunk 0 + tail + a mid-file probe) for ANY blob size, without letting a huge blob's chunks accumulate. Entries carry the layout signature {total, chunkSize} and reads validate it: re-vaulting an item (moveItemToVault on an EXISTING id) replaces the blob under the same id, and a stale chunk would serve WRONG PLAINTEXT - the signature must match or the chunk is dropped and re-fetched. clearPlaintextCache() clears both caches (plaintext must never outlive the key). Size read from the DB: file_size on the encrypted row is the ENCRYPTED size (150,727,825 for 9cc25ea7); the plaintext total comes from the IVG1 header at serve time.
- **big terabox-only blob: parallel chunk fetch + byte-budget cache (2.12.87, 2026-09-18)**: 2.12.86's chunk granule cache did NOT fix 9cc25ea7 - SW logs showed every read succeeding (	erabox fetch done: 206 bytes 100663648-109052283/150728345, layout ok {total: 150727825, chunkSize: 8388608}) then the client aborting. The real constraint is ARITHMETIC, not caching: terabox caps ONE dlink at ~0.68MB/s, so 143.7MiB = ~210s of CDN time, and the preview's metadata budget is 180s - it died at 180s no matter what was cached (the 23MiB item worked because ~34s fits). The old _decryptFullVaultBlob fetched the full encrypted span in ONE request on ONE dlink, so the whole-blob path was capped at 0.68MB/s too. Fix (1): _decryptFullVaultBlob now fetches chunks in PARALLEL batches (terabox=3, others=6; the ~0.68MB/s cap is PER dlink CONNECTION per rclone multistream evidence, so 3 concurrent reads give ~2MB/s aggregate) -> 143MiB drops to ~70s, under budget, and each batch overlaps the next resolve with the previous transfer. Any chunk failure rejects the whole decrypt loudly (Promise.allSettled + first rejection), never a partial buffer. Fix (2): the 64MiB whole-blob cap excluded this item entirely - replaced the fixed item-count LRU with a byte budget: MAX_ENTRY 192MiB (gate), MAX_BYTES 192MiB (eviction), never evicting the entry just added (one big entry is exactly what the budget permits). Memory is bounded at ~192MiB steady + transient batch buffers.
- **single-frame preview: fetch the moment, not the movie (2.12.88, 2026-09-18)**: the user's call — a preview is a spec of a moment; downloading 143MiB to recover one frame is wrong. Root cause of the old behavior: runExtraction mounted a <video> on the vault-stream HTTP endpoint and sought, which (a) forces the media engine to probe for the moov atom by aborting+reopening range requests (each reopen re-pays terabox dlink resolves), and (b) for blobs <=192MiB the whole-blob plaintext cache decrypted the ENTIRE file first. Both are gone. New path, all verified against a real ffmpeg-generated faststart MP4 (frame-extraction hash EXACTLY matches ffmpeg's own select=eq(n,90) output): (1) page resolves copies (existing getPreviewStreamUrl), (2) new SW message vaultFetchPlaintextRange{id,copies,fileName,start,end} downloads+decrypts ONLY the encrypted chunks covering [start,end] — a 143MiB file costs ~16MiB of host reads instead of 143MiB; (2b) probe the head (one chunk) for moov, fall back to the tail chunk only if moov is absent there (non-faststart); (3) new utils/mp4MinFrame.js parses moov (mvhd/mdhd/hdlr/stts/stsc/stco/stsz/stss), maps the target ratio -> tick -> sample index, snaps BACKWARD to the nearest sync sample (a P-frame without its references decodes to nothing), and computes the sample's absolute byte range; (4) range-fetch just that sample and rebuild a tiny valid MP4 around it via buildSingleFrameMp4 (original stsd copied VERBATIM — it carries avcC/SPS/PPS the decoder needs; stco patched by BOX SCAN not hand-summed header math — that arithmetic silently drifts); (5) the synthetic 1-frame MP4 is decoded by a <video> and rasterized to the scored JPEG. There is NO moov-probe churn anymore (no streaming mount at all) and NO whole-blob decrypt for previews. mp4MinFrame is deliberately narrow and LOUD: fragmented MP4 (mvex marker), missing sample tables, or absent moov all reject explicitly — no fallback to 'download everything', a preview that cannot be located cheaply fails visibly. Verified end-to-end with ffmpeg + ffprobe on testsrc; caught one real bug in testing: stsc entries are 12 bytes (first_chunk, samples_per_chunk, sample_desc_index) but the rebuilder allocated 16 total, dropping the third field — decode failed with 'Cannot determine format' until fixed. The plaintext-cache machinery from 2.12.85-87 stays for the detail PLAYER (real streaming), it's just no longer on the preview path.
- **single-frame preview: moov at the END (non-faststart) + the 16-byte header was re-read every call (2.12.89, 2026-09-18)**: 2.12.88 shipped, then the last terabox-only item (9cc25ea7, 143.7MiB, video/mp4) STILL showed 'preview unavailable' while SW logs showed three bytes=0-15 header reads then one chunk-0 read and nothing after. Three real bugs, all found by reproduction: (1) **the tail slice is not box-aligned**. ffmpeg's DEFAULT writes moov at the END of the file (faststart is a separate flag), so most real-world videos need the tail read — and fetchVaultPlaintextRange returns a slice starting at an arbitrary offset, MID-BOX. The old findMoov did a strict box walk from slice offset 0, which parses garbage and silently returns null -> 'moov not found in head or tail'. Fix: findMoov now SCANS for a plausible moov header (4-byte size + 'moov' type) and VALIDATES it holds mvhd + trak before accepting — false positives on random H.264 bytes are then effectively impossible, and a mid-box slice no longer matters. Verified with a real non-faststart file (moov absent from a 64KiB head, found in a deliberately misaligned 30KiB tail, rebuilt single-frame MP4 decodes). (2) **the rebuilt MP4 had handler_type at the wrong offset** — hdlr is [version/flags 4][pre_defined 4][handler_type 4][reserved 12][name], so handler_type is at payload offset 8, NOT 4; 'vide' at offset 4 still plays in lenient decoders but is invalid ISO-BMFF and a strict parser sees a non-video track and skips it. Same class of bug in mvhd (next_track_ID at 92 not 96, payload 96 not 100), tkhd (duration at 20 not 16 — a 4-byte reserved sits between track_ID and duration; volume at 32), mdhd (payload 24 not 20, language at 20 not 18). All fixed to spec offsets; the rebuilder now round-trips (locateVideoFrame parses its own output back, which is the test that caught the hdlr bug). (3) **the 16-byte IVG1 header was fetched on EVERY plaintext-range call** — getPreviewStreamUrl probes, runExtraction probes again, and every fetchVaultPlaintextRange re-read the header, and on terabox each read is a full dlink resolve + fetch for 16 BYTES, so a single preview burned 4+ resolves before touching a frame. Fix: the layout is immutable per blob, so it is memoized per item (this.vaultBlobLayout) and read ONCE per unlock; probeVaultBlobFormat and fetchVaultPlaintextRange both go through _resolveBlobLayout. Also wired the 2.12.86 chunk-granule cache into fetchVaultPlaintextRange so a frame landing in an already-read chunk (head + frame often share chunk 0) costs nothing. Invalidated on re-vault/restore/upload via _invalidateVaultBlobCaches (layout + plaintext + chunk + URL caches keyed by that id) and cleared on lock with the rest — a re-vault replaces the blob under the SAME id and a stale layout would serve wrong plaintext. runExtraction now logs every step (layout, head size, moov-in-head-or-tail, located frame offset/size/dimensions) so the next failure names itself instead of ending at a blank console.
- **single-frame preview: the 64MiB message cap + moov bigger than the window (2.12.90, 2026-09-18)**: 9cc25ea7 (143.7MiB, terabox-only) kept failing with 'Message exceeded maximum allowed size of 64MiB' from useChromeMessage. That is the SAME 64MiB channel cap from 2.12.21: the preview's frame fetch returned a bogus ~67MiB range (chunks 6-14 pulled for one 'frame' — a real video sample is never bigger than ~2MiB). Root cause: the 8MiB head/tail window TRUNCATED moov (a long video's per-frame sample tables can exceed 8MiB), and the old locator parsed the cut-off tables anyway — the box walk ran past the real data into unrelated bytes and produced a plausible-looking but garbage sample size. Three fixes, all verified against real ffmpeg output: (1) findMoov prefers a COMPLETE moov (off+size <= window) and only accepts a truncated one as a last resort with a loud warning — it also scans by direct byte compare now (a 32MiB window is 32M iterations, String.fromCharCode per step was slow); (2) runExtraction's moov window GROWS (1 chunk, then 2, 3, 4, capped at 32MiB, head first then tail, chunk-cache makes re-reads free) until moov fits completely — a truncated moov is never parsed; (3) defense in depth: locateVideoFrame rejects any 'sample' over 4MiB (MAX_SAMPLE_BYTES, loud warn), and fetchVaultPlaintextRange hard-caps responses at 40MiB (under the 64MiB channel cap but room for the 32MiB window) with a message that names the bogus range. Verified: truncated-moov fixture is detected and rejected (no multi-MiB sample emitted), faststart round-trip re-parses its own output (caught the hdlr handler_type-at-offset-8 bug plus mvhd/tkhd/mdhd field-offset bugs — all fixed to spec), misaligned-tail moov still found.
- **vault preview: chunk the 64MiB channel (2.12.91, 2026-09-18)**: 9cc25ea7 kept dying with 'Message exceeded maximum allowed size of 64MiB' on the VERY FIRST head fetch (8MiB, well under the limit). Chrome's structured-clone for typed arrays can bloat past the limit even for an 8MiB Uint8Array (observed). Fix: the page no longer sends one 8-32MiB range per moov/frame read; fetchPlain now splits any range larger than 1MiB into 1MiB sendMessage calls and reassembles. The background's 8MiB encrypted chunk is fetched once (granule cache makes the slices free) and each 1MiB response stays comfortably under the channel cap. Added per-chunk request/response logging so the next failure names the exact range.
- **vault preview: diagnostic trace for empty slice (2.12.92, 2026-09-18)**: 9cc25ea7's head fetch (8MiB window, chunked as 1MiB messages to stay under the 64MiB channel cap) returned 'no bytes' for the very first slice 0-1048575. Added per-chunk logging in the service worker (fetchPlaintextRange: requested range, chunks, cache hit/miss, encrypted fetch size, decrypted size, slice offsets) so the next run names the exact failing step in the service worker console (edge://extensions -> Inspect views: service worker).
- **vault preview: normalize structured-clone for typed arrays (2.12.93, 2026-09-18)**: 9cc25ea7's head fetch returned 1MiB from the service worker (background log: 'returning 1048576B') but the page saw 'no bytes' — the Uint8Array was not surviving the structured clone as a Uint8Array (Chrome deserialized it as a plain object with numeric keys or ArrayBuffer). Added normalizeBytes helper that handles Uint8Array, ArrayBuffer, Array, and plain-object cases, with per-chunk type/length logging. Keeps the 1MiB chunking from 2.12.91.
